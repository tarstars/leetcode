use crate::Solution;

impl Solution {
    pub fn insert(intervals: Vec<Vec<i32>>, new_interval: Vec<i32>) -> Vec<Vec<i32>> {
        let _ = (intervals, new_interval);
        todo!("implement Solution::insert")
    }
}
