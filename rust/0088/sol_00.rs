use crate::Solution;

impl Solution {
    pub fn merge(nums1: &mut Vec<i32>, m: i32, nums2: Vec<i32>, n: i32) {
        let _ = (nums1, m, nums2, n);
        todo!("implement Solution::merge")
    }
}
